# Vocaboot UX 改善バックログ（100）

開発時に DOM に出る `<nextjs-portal>` は Next.js の開発用オーバーレイ用ノードです。`next.config.ts` の `devIndicators: false` で画面隅の Dev インジケーターを消せます。`next start` の本番ビルドではユーザー向け DOM に載りません。エラー時オーバーレイ用の portal は開発時に残り得ます。

## 洗い出し（優先度は上ほど検討しやすい）

1. ホーム統計の読み込み中スケルトン表示
2. 学習セッションの評価ボタン連打防止
3. 保存失敗時のトーストフィードバック
4. キーボード 1〜4 で評価ショートカット
5. スクリーンリーダー向け `aria-live` で語切替を通知
6. メインへのスキップリンク
7. 復習ページの件数読み込み表示
8. オフライン・IndexedDB 障害時のリカバリ手順表示
9. セッション離脱確認（未評価のまま戻る）
10. 学習中断位置のローカル記憶と再開
11. リスト全体の検索・ジャンプ
12. タグ／品詞フィルタでセッション開始
13. カスタム語数（5/15/20）プリセット
14. ホーム CTA のコピー A/B（文言テスト）
15. 完了画面で直前セットの要約（評価内訳）
16. 復習予定の一覧（次回日付つき）
17. ストリーク・連続日数表示
18. 日別学習グラフ（軽量）
19. エクスポート（JSON/CSV）
20. インポート・端末移行
21. 設定に「ハプティクス」トグル（対応端末）
22. 和訳表示のアニメーション（控えめ）
23. 大文字フォーカスモード（ディスレクシア配慮）
24. フォントサイズ段階設定
25. アニメーション全オフ（既存 reduced-motion の拡張確認）
26. ダークモードのコントラスト監査（金×文字）
27. ヒーロー領域をテーマ連動にするオプション
28. PWA アイコン・スプラッシュの再確認
29. `manifest` の `theme-color` と実表示の一致確認
30. 底ナビのラベル短縮（極小画面）
31. 横持ち時のレイアウト
32. タブレット `max-w` の段階拡大
33. 学習中のブラウザバック挙動
34. セッション URL の共有時の種明かし防止（seed）
35. パブリック端末向け「データ消去」設定
36. 英語 TTS の声・速度設定
37. TTS 未対応ブラウザの文言
38. 例文の音声読み上げ
39. 語の発音記号表示（データがあれば）
40. 誤訳報告フロー（将来）
41. 例文コピーボタン
42. 語を Long press で辞書検索（外部）
43. 忘却曲線に基づく別アルゴリズムトグル
44. 「復習間隔を詰める」の数値プレビュー
45. 設定変更の即時ツールチップ
46. 英語キーボード配列ヒントの表示切替
47. ナビのアクティブ状態をルートごとに音読
48. 404 ページの学習導線
49. ネットワークエラー境界コンポーネント
50. Service Worker 更新トースト（PWA）
51. 初回チュートリアル（1 枚オーバーレイ）
52. 権限説明（ストレージ）のプライバシ短文
53. 利用規約・OSS ライセンス画面
54. ホームの「通勤向け」バッジを設定で非表示
55. 語リスト進捗バー（全体の何％）
56. ミックスのシード再シャッフル明示ボタン
57. セッション完了後の SNS 共有文（任意）
58. ロック画面ウィジェット（将来・OS 依存）
59. Siri ショートカット／アプリショートカット（将来）
60. ダウンロード帯域削減（辞書データ分割）
61. 初回データ展開のプログレス
62. メモリ使用量の見積り表示（デバッグ）
63. 多言語 UI（英語）
64. 日付ロケール表示
65. タイムゾーンと SRS 締めの説明
66. アカウント不要方針の明示
67. 学習リマインダー（通知 API・慎重に）
68. バッジ通知数（PWA）
69. iOS セーフエリア下の余白再監査
70. Android Chrome のアドレスバー表示時レイアウト
71. ダブルタップズーム抑止（語カード）
72. スクロール容器の overscroll 挙動
73. カードの影のテーマ一貫性
74. ボタンの最小タッチ 44px 監査
75. `prefers-contrast` 対応
76. 入力欄追加時のアクセシビリティ監査
77. フォーカストラップ（将来モーダル）
78. シート／ドロワーで語詳細（将来）
79. 単語帳お気に入り
80. 自作例文メモ（ローカル）
81. 画像連想フラッシュカード（将来）
82. リスニングモード（自動次へ）
83. タイムアタックモード
84. 間違いノート自動収集
85. 再テストだけのセッション
86. 直近 N 語の履歴スタック
87. Undo 直前の評価
88. 二段階確認（「簡単」連打防止拡張）
89. セッションタイマー表示
90. 目標語数/日（設定）
91. 達成祝いマイクロコピー
92. データ破損検出とリセット導線
93. idb-keyval 失敗時のフォールバック（localStorage）
94. E2E（Playwright）クリティカルパス
95. ビジュアルリグレッション（任意）
96. Core Web Vitals の計測
97. CSP ヘッダ検討
98. サードパーティスクリプト最小化監査
99. エラー報告 ID のユーザー表示
100. このリスト自体の月次見直しフロー

---

## 実装ログ（追記用）

- **2026-04-02**: 1,2,3,4,5,6,7 と `devIndicators: false` を反映。
- **2026-04-02**: 9（戻る時の確認ダイアログ・タブ閉じる警告）・10（チェックポイント保存・ホーム「続きから」・7日で無効）を反映。
